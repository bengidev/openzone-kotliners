package io.github.bengidev.openzone.onboarding.domain

/**
 * Enumeration of onboarding page types.
 * Each type represents a distinct feature showcase with its own visual demo.
 */
enum class OnboardingPageType {
    EncryptedPairing,
    IdeaStudio,
    PromptQueue,
    ReasoningControl,
    WorkspaceReady
}
